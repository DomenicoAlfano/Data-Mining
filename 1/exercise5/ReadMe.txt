In the 5th exercise is important to install the gmplot library following the command 'pip install gmplot' and then replace the folder created from the installation with the submitted one.

Note that the version used is Python 3.6
